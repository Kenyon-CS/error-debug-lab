# One port only
PORT=4111

# MariaDB connection
DB_HOST=127.0.0.1
DB_USER=root

# INTENTIONAL BUG (Bug 1):
# The server code (server/src/config.js) expects DB_PASS, but we provide DB_PASSWORD here.
DB_PASSWORD=your_password_here

DB_NAME=debug_lab
